# Tool Boundaries — TravelPlannerAgent

> Auto-generated by /boundary on 2026-04-02
> Source: docs/tool-routing-report.md (approved 2026-04-02)
> Update by running /boundary again after modifying the routing report.

## Project Scope Declaration

This project uses the following tools. Skills not listed here are global noise — deprioritize them.

### Skills

#### sentinel:progress
- **Purpose in this project:** Session logging and progress.yaml maintenance
- **Can do:** Generate structured progress.yaml entries with typed candidates
- **Cannot do:** Modify project source code or architecture docs
- **Input constraints:** Needs completed work context from current session
- **Failure behavior:** Manually append entry to progress.yaml

#### sentinel:sentinel-export
- **Purpose in this project:** Export compliance docs and session reports
- **Can do:** Render project docs to md/pdf/docx, lint for AI patterns
- **Cannot do:** Publish to external systems
- **Input constraints:** Needs docs/ directory with content
- **Failure behavior:** Manual export

#### codex:rescue
- **Purpose in this project:** Codex-assisted review and diagnosis (Stage 5 only)
- **Can do:** Delegate review or diagnostic tasks to Codex CLI
- **Cannot do:** WebSearch (tools allowlist is Bash only), write to project files
- **Input constraints:** Codex CLI must be installed
- **Chain pattern:** Generate artifact → codex:rescue review → parse feedback → iterate
- **Failure behavior:** Use Claude self-review or skip enrichment step

#### codex:adversaril-review
- **Purpose in this project:** Stage 5 structured review of generated YAML/JSON artifacts
- **Can do:** Review itinerary/restaurant/hotel artifacts for quality, consistency, feasibility
- **Cannot do:** Modify source artifacts
- **Input constraints:** Requires artifacts to exist in sessions/{session_id}/
- **Chain pattern:** artifacts → codex:review → review-report.json
- **Failure behavior:** Fall back to sentinel:call-codex or Claude self-review

#### codex:result
- **Purpose in this project:** Parse and present Codex review results in structured format
- **Can do:** Format Codex output into actionable accept/flag/reject items
- **Cannot do:** Execute remediation actions
- **Input constraints:** Raw Codex review output
- **Failure behavior:** Manually parse Codex output

#### original:web-extractor
- **Purpose in this project:** Extract POI, restaurant, and hotel data from web pages (Stage 2/4)
- **Can do:** Extract structured text from JavaScript-rendered pages, SPAs, lazy-loaded content
- **Cannot do:** Bypass paywalls or CAPTCHAs
- **Input constraints:** Needs a URL to extract from
- **Chain pattern:** WebSearch finds URL → web-extractor gets content → parse into candidate YAML
- **Failure behavior:** Fall back to WebSearch snippet data only

### MCP Tools

#### Notion MCP (mcp__notion__*)

| Tool | Can do | Cannot do | Input constraints |
|------|--------|-----------|-------------------|
| notion-search | Find travel planner pages/databases by title | Search by content within pages | Query string |
| notion-create-pages | Create parent travel plan page and POI/restaurant/hotel entries | Create more than 100 items per call | Page properties + content blocks |
| notion-create-database | Create Itinerary/Restaurants/Hotels/Notices databases | Create views (use notion-create-view) | Parent page ID + property schema |
| notion-create-view | Create board view (by day) or table view for databases | Modify existing views | Database ID + view config |
| notion-update-page | Update existing POI/restaurant/hotel entries | Batch update (one page per call) | Page ID + changed properties |
| notion-fetch | Read page/database content for verification | Write anything | Page ID or URL |
| notion-get-comments | Read review comments on travel plan pages | Create comments (use notion-create-comment) | Page ID |
| notion-create-comment | Add review notes to travel plan pages | Modify existing comments | Page ID + comment body |

**Chain pattern:** Create parent page → create 4 databases → batch-create entries → create views → fetch to verify
**Key boundary:** One-way publish only. Never read from Notion to update local SQLite. Batch limit: 100 items per create call.

#### Playwright MCP (mcp__plugin_playwright_playwright__*)

| Tool | Can do | Cannot do | Input constraints |
|------|--------|-----------|-------------------|
| browser_navigate | Open Notion page URL for screenshot | Navigate to non-Notion URLs in this project | Full URL |
| browser_take_screenshot | Capture Notion database views for visual verification | Capture content behind auth walls without login | Page must be loaded |
| browser_snapshot | Get DOM state of Notion page for structural verification | Modify page content | Page must be loaded |
| browser_wait_for | Wait for Notion page to finish rendering before screenshot | Wait indefinitely | Timeout value |

**Chain pattern:** navigate to Notion URL → wait_for render → take_screenshot → Codex reviews screenshot
**Key boundary:** Read-only verification. Never click, type, or modify Notion content via Playwright.

#### Computer-Use MCP (mcp__computer-use__*)

| Tool | Can do | Cannot do | Input constraints |
|------|--------|-----------|-------------------|
| screenshot | Capture desktop state showing Notion page | Capture without app in foreground | App must be visible |
| open_application | Bring Notion or browser to foreground | Install applications | App name |
| scroll | Scroll through long Notion pages to capture all sections | Modify content | Direction + amount |

**Chain pattern:** open_application (browser) → screenshot → scroll → screenshot (repeat for full page)
**Key boundary:** Fallback for Playwright. Read-only capture only. Never type, click, or modify content via computer-use.

### CLI Tools

#### claude -p (pipe mode)
- **Purpose in this project:** Structured transformation of raw data into schema-enforced JSON output
- **Invocation:** `claude -p "transform prompt" --bare --output-format json --json-schema "$(cat contract.json)" --max-turns N`
- **Constraints:**
  - Always use `--bare` to prevent `.mcp.json` auto-discovery (BUG-004 mitigation)
  - One JSON Schema per call (structured output)
  - Schema complexity: ≤20 strict tools/request, ≤24 optional params total, ≤16 anyOf unions
  - Use `--max-turns` and `--max-budget-usd` to bound each stage
  - Extract result via `jq '.structured_output'`
  - Do NOT use `--allowedTools WebSearch` — search is handled by `/call-codex`, not `claude -p`

#### Codex CLI
- **Purpose in this project:** Soft-judgment review of generated artifacts
- **Invocation:** Via codex:rescue or  codex:adversaril-review
- **Constraints:** Async execution; pipeline waits for completion. Output is advisory, not authoritative.

#### Prototype CLI (trip command)
- **Purpose in this project:** Read/write to SQLite data layer
- **Invocation:** `python3 -m tripdb.cli.trip {command} {args}` from project root
- **Constraints:** This project reads via CLI and SQL views. Never write SQL directly. All 11 commands available (see .claude/rules/cli-write-layer.md).

## Escape Hatch

If a routing rule or boundary declaration causes repeated failures (3+ consecutive), fall back to asking the developer rather than following the stale rule. Report the failure pattern so the developer can update the routing report.
